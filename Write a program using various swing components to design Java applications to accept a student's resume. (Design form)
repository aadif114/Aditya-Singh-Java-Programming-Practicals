import javax.swing.*;
import java.awt.*;

public class s112pra5 extends JFrame {

    JLabel lblName, lblEmail, lblMobile, lblGender, lblCourse, lblAddress, lblSkills, lblHobbies;
    JTextField txtName, txtEmail, txtMobile, txtHobbies;
    JTextArea txtAddress;
    JRadioButton rbMale, rbFemale;
    ButtonGroup bg;
    JComboBox<String> cmbCourse;
    JCheckBox cbJava, cbPython, cbCpp;
    JButton btnSubmit, btnReset;

    s112pra5() {

        setTitle("Student Resume Form");
        setSize(550, 700);
        setLayout(null);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);

        // ===== Heading =====
        JLabel heading1 = new JLabel("S112 Aditya");
        heading1.setBounds(20, 10, 250, 30);
        heading1.setFont(new Font("Arial", Font.BOLD, 22));
        add(heading1);

        JLabel heading2 = new JLabel("STUDENT RESUME", SwingConstants.CENTER);
        heading2.setBounds(100, 40, 320, 35);
        heading2.setFont(new Font("Arial", Font.BOLD, 24));
        add(heading2);

        // Name
        lblName = new JLabel("Name");
        lblName.setBounds(30, 90, 100, 25);
        add(lblName);

        txtName = new JTextField();
        txtName.setBounds(150, 90, 250, 25);
        add(txtName);

        // Email
        lblEmail = new JLabel("Email");
        lblEmail.setBounds(30, 130, 100, 25);
        add(lblEmail);

        txtEmail = new JTextField();
        txtEmail.setBounds(150, 130, 250, 25);
        add(txtEmail);

        // Mobile
        lblMobile = new JLabel("Mobile");
        lblMobile.setBounds(30, 170, 100, 25);
        add(lblMobile);

        txtMobile = new JTextField();
        txtMobile.setBounds(150, 170, 250, 25);
        add(txtMobile);

        // Gender
        lblGender = new JLabel("Gender");
        lblGender.setBounds(30, 210, 100, 25);
        add(lblGender);

        rbMale = new JRadioButton("Male");
        rbMale.setBounds(150, 210, 80, 25);

        rbFemale = new JRadioButton("Female");
        rbFemale.setBounds(240, 210, 100, 25);

        bg = new ButtonGroup();
        bg.add(rbMale);
        bg.add(rbFemale);

        add(rbMale);
        add(rbFemale);

        // Course
        lblCourse = new JLabel("Course");
        lblCourse.setBounds(30, 250, 100, 25);
        add(lblCourse);

        String courses[] = {"B.Sc CS", "BCA", "B.Com", "BBA", "MCA"};
        cmbCourse = new JComboBox<>(courses);
        cmbCourse.setBounds(150, 250, 250, 25);
        add(cmbCourse);

        // Skills
        lblSkills = new JLabel("Skills");
        lblSkills.setBounds(30, 290, 100, 25);
        add(lblSkills);

        cbJava = new JCheckBox("Java");
        cbJava.setBounds(150, 290, 70, 25);

        cbPython = new JCheckBox("Python");
        cbPython.setBounds(220, 290, 80, 25);

        cbCpp = new JCheckBox("C++");
        cbCpp.setBounds(310, 290, 70, 25);

        add(cbJava);
        add(cbPython);
        add(cbCpp);

        // Address
        lblAddress = new JLabel("Address");
        lblAddress.setBounds(30, 330, 100, 25);
        add(lblAddress);

        txtAddress = new JTextArea();
        JScrollPane sp = new JScrollPane(txtAddress);
        sp.setBounds(150, 330, 250, 100);
        add(sp);

        // Hobbies
        lblHobbies = new JLabel("Hobbies");
        lblHobbies.setBounds(30, 450, 100, 25);
        add(lblHobbies);

        txtHobbies = new JTextField();
        txtHobbies.setBounds(150, 450, 250, 25);
        add(txtHobbies);

        // Buttons
        btnSubmit = new JButton("Submit");
        btnSubmit.setBounds(150, 520, 100, 35);
        add(btnSubmit);

        btnReset = new JButton("Reset");
        btnReset.setBounds(280, 520, 100, 35);
        add(btnReset);

        setVisible(true);
    }

    public static void main(String[] args) {
        new s112pra5();
    }
}
